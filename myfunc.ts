import { parse } from "querystring";

export function myfunction()
{
    school_name:String;
    const school_name=parse((< HTMLInputElement > document.getElementById("serachschool")).value);
    

}

var obj=[{}];
obj.name=name;
const name=parse((< HTMLInputElement > document.getElementById("name")).value);;
const=parse((< HTMLInputElement > document.getElementById("serachschool")).value);;


